# 验证记录

DeepSeek 已接通并成为活动后端：原海报 1.83 秒、用户苹果截图 1.73 秒成功返回。格式包装问题已修复，24 项回归测试通过。准确性与截图测试边界见 [DeepSeek 验证](DEEPSEEK.md)。

最新状态建议与真机安装结果见 [状态建议验证](STATE-ADVICE.md)：21 项后端测试、18 项 Swift 检查通过，新版已在 iPhone 启动；新提示词的真实照片内容质量仍待验证。

最后更新：2026-09-14。区分源码检查、固定样例、实际运行及真实模型结果；后续验证不计作赛事 100 分钟内完成。

## 已通过

| 检查 | 结果 | 验证边界 |
| --- | --- | --- |
| 后端单元与 HTTP 集成 | 17 项通过 | 上游使用模拟响应；本机 HTTP 服务真实收发 |
| Swift 核心逻辑 | 编译运行，15 项通过 | 图片坐标、菜单位置、独立物体动作及内容 |
| iOS 完整编译 | iPhone 未签名目标、模拟器目标均 BUILD SUCCEEDED | Xcode 26.6 / iOS 26.5 SDK |
| iOS 实际启动 | 已安装并启动于 iPhone 17 Pro 模拟器 | 实际界面显示品牌、拍照与相册入口；不是运行在实体手机上 |
| iOS 相机拒绝 | 拒绝权限后正确提示前往设置或改用相册 | 未验证真实相机拍摄 |
| 浏览器手机布局 | 393 × 852 视口下检查 | 示例框与菜单可见，桌面浏览器尺寸模拟不能替代 iPhone Safari |
| 浏览器物体切换 | 书与杯子显示不同动作及内容 | 明确标注的人工示例，无模型调用 |
| 浏览器结果与复制 | localhost 下复制书籍文字，与剪贴板内容核对通过 | 比较时统一换行符；局域网 HTTP 不保证自动复制 |
| HTTP 复制回退 | 显示可全选、长按复制的文字区域 | 不再依赖返回值可能不可靠的旧复制接口，也不误报成功 |
| HTTP 分享回退 | 显示分享内容和复制入口 | 尚未验证手机原生系统分享面板 |
| 浏览器真实图片输入 | 文件选择器上传参考海报，显示压缩后的实际照片 | 后端返回模型繁忙；没有出现虚构物体框 |
| 静态资源隔离 | 正常页面与图片可读，私有配置及路径穿越请求返回 404 | 不开放项目目录浏览 |

## 当前：切回 Gemini（2026-09-14）

不再使用智谱，切回原有 Gemini。已将本地 `VISION_PROVIDER` 改为 `gemini` 并重启后端；原有 Gemini 密钥和 `gemini-3.7-flash` 模型设置均保留。当前识别不会调用智谱，先前关于智谱标准模型的待授权验证已不再继续。

本轮直接请求原模型，获得以下真实结果（保持相同密钥）：

- 原海报、30 秒上限：30.01 秒中止，TIMEOUT。
- 最小文字请求：19.40 秒 HTTP 200，产生文字输出。
- 原海报、仅延长到 60 秒：45.37 秒 HTTP 200，返回并通过校验的 1 个物体。尚未逐字段核对识别内容与物体框。
- 修复后再次请求原海报：20.79 秒 HTTP 503 / UNAVAILABLE，明确返回 “This model is currently experiencing high demand.”。

结论：本地等待上限不足与 Gemini 间歇性繁忙叠加；不是仅凭模型列表推定可用。后端默认及本地配置改为 60 秒，网页与 iOS 源码改为 75 秒；503 单独显示模型繁忙。后端已重启；手机上的旧安装需要重新构建安装才会采用新等待时间。不能保证上游每次成功，不宣称 503 已被修复。

复现工具：`node backend/diagnose-request.mjs original`；最小文字对照用 `text` 参数。工具仅在手动执行时调用当前 Gemini，输出耗时、HTTP 状态和脱敏错误，不输出密钥或图片内容，不调用智谱。

## 历史：智谱真实调用

按智谱官方接口文档配置密钥，本地选择 `glm-5.3-flash`。第一次请求返回 400 / 1210，说明该模型不能关闭思考。适配已改为 GLM 5 系列启用思考并使用 low effort；该行为有回归测试。

后续真实调用返回限流。使用文档中的免费视觉模型 `glm-4.6v-flash` 做一次对照，也返回 HTTP 429 / 1305：该模型当前访问量过大，请稍后再试。网页上传参考海报时也真实收到服务繁忙错误。

2026-09-14 使用当前配置再次做一次检查，返回 NETWORK_ERROR。当时尚未获得成功识别结果；后续 Gemini 结果见上方当前记录。

标准模型 `glm-4.6v` 可能收费且无明确授权，对应验证未执行。

所有记录均不输出密钥。配置切换仅在后端生效，iOS 与网页共用相同物体数据接口。

## 历史 Gemini 限制

之前尝试 Gemini 时，服务返回 HTTP 400 / FAILED_PRECONDITION：User location is not supported for the API use. 后端保留此错误映射和 Gemini 适配；不通过更换网络区域等方式规避服务限制。

## iOS 环境及待验证项

完整 Xcode 位于 `/Applications/Xcode.app`。系统默认开发工具路径仍指向 Command Line Tools，构建命令使用 `DEVELOPER_DIR` 指定完整 Xcode。iOS 26.5 arm64 运行组件已安装，模拟器应用已实际启动并通过界面确认。

此前 iPhone 15 Pro Max 已配对、开启开发者模式，并选定个人开发团队。原先签名流程遇到缺少平台组件和没有注册设备描述文件的错误；平台组件如今已安装，但尚未完成实体手机签名和安装。未尝试收费模型或购买任何服务。

仍需完成：

- 实体 iPhone 上的开发签名、安装、相机和相册流程。
- iPhone Safari 访问同一局域网后端及拍照、复制兼容性确认。
- 可用识别服务下取得真实结果，检查两物体同框、横竖图坐标和识别内容。
- 实际网络请求中换图后过滤迟到响应的端到端检查。
- 系统分享及成功识别录屏。

## 记录位置

本地 `.build/` 包含 Xcode 构建日志、Swift 检查产物及模型请求状态记录；不收入源码压缩包。`web/demo-data.mjs` 与示意图为人工样例，界面和分享内容均注明来源。参考海报底部被遮挡的文字没有写入样例识别答案。

## iPhone 安装更新（2026-09-14）

iPhone 15 Pro Max 已连接。使用现有个人团队自动签名构建成功；本机 codesign 校验报告 CSSMERR_TP_NOT_TRUSTED，devicectl 确认最新版安装成功（com.rightclickreality.demo）。尝试启动时系统以 Security 拒绝，提示签名、权限或尚未显式信任开发者；需要用户在手机上检查并信任开发者。尚未确认真机 App 启动或拍照识别成功。此前“未完成签名安装”的记录为历史状态。
