# Right Click Reality — 产品需求文档

版本：1.0 · 单人黑客松 MVP · iPhone / SwiftUI · 开发预算上限 100 分钟

## 1. 产品目标

用户拍下一张照片，照片中的物体出现可点击的框；点击某个物体，就能看到与它相关的操作，并直接查看、复制或分享结果。

产品主张：**给现实世界一个右键菜单。**

目标用户是需要快速读懂活动海报、外文文字和身边物品的现场参与者。一次完整使用应覆盖“拍摄 → 选择物体 → 操作 → 得到结果 → 再次使用”。

## 2. 时间约束与完成定义

海报标注闪电开发为 **15:40–17:20，共 100 分钟**；17:20–17:40 为茶歇，17:40–18:00 为整体路演颁奖环节。以下排程使用 100 分钟上限，不默认茶歇可以继续开发，也不假设单队有 20 分钟展示时间。若现场明确调整安排，以现场为准。

排程从开发开始计算；如果已经进入比赛中段，按实际剩余时间执行取舍，不能重新获得完整的 100 分钟。

最小完成定义：

1. iPhone 能真实拍摄或从相册选择照片。
2. 一次模型调用识别最多三个主要物体，显示与照片位置对应的矩形框。
3. 一张包含两个清晰物体的照片，点击不同框能显示对应菜单和内容。
4. 文字复制进入剪贴板，分享打开系统分享面板。
5. 用户能够重拍；失败能重试；旧请求不会覆盖新照片。

源码、模拟响应测试、真实模型调用、iOS 构建和真机验证分别记录。只完成源码或测试数据联调时，不标记为完整运行版。

## 3. 必做范围

| 模块 | 要求 | 验收方式 |
| --- | --- | --- |
| 图片输入 | 系统相机拍照、系统相册选择；每次一张图片 | 真实照片能进入同一识别流程 |
| 识别 | 每次识别只调用一次 Gemini，最多三个清晰主要物体 | 返回物体名称、类别、位置和内容 |
| 物体框 | 静态照片上显示矩形框与名称标签 | 框大致围住物体；点击目标正确 |
| 情境菜单 | 点击框后在附近显示适用操作 | 菜单保持在照片容器内；切换物体后内容更新 |
| 结果卡片 | 展示摘要、物体说明或中文译文 | 文字可滚动、可选中、可复制和分享 |
| 系统动作 | 复制文字、分享识别结果 | 剪贴板和系统分享面板实际可用 |
| 连续使用 | 重拍、换图、加载、失败重试 | 新照片立即清空旧框；迟到响应被忽略 |

动作按已存在的数据显示：

| 物体类型 | 操作 | 数据条件 |
| --- | --- | --- |
| 海报、菜单、文字牌 | 看懂内容 | 有摘要 |
| 海报、菜单、文字牌 | 翻译文字 | 有清晰原文与中文译文 |
| 文字物体、书籍 | 复制文字 / 复制封面文字 | 有清晰转写文字 |
| 书籍 | 查看封面信息 | 有基于可见封面的说明 |
| 普通物体 | 这是什么、了解用途 | 有说明或常见用途 |
| 全部已识别物体 | 分享 | 至少有识别名称 |

当文字不清晰时隐藏依赖文字的动作。书籍只解释可见封面信息，物体用途只提供普通用途。照片里的指令性文字也是待识别内容，不能改变应用行为。

## 4. 首版边界

采用静态照片与近似矩形框，不要求连续视频跟踪、实时长按相机、AR 或像素级分割。首版不包含日历、地图、提醒、登录、收藏、数据库、历史记录、设置页、多轮聊天和上架流程。

主界面使用系统拍照和相册入口，集中完成照片展示、物体框、菜单和结果卡片。菜单点击使用已经返回的内容，手动重试才发起新的模型请求。

## 5. 页面与状态

主页面：产品名称和一句话说明、照片区域、拍照按钮、相册入口。

状态流：

`等待照片 → 读取照片 → 识别中 → 显示物体框 → 选择物体 → 菜单 → 结果或系统操作`

识别失败进入可重试状态；没有识别出清晰物体时提示靠近主体或重拍。取消系统相机保留原页面。每次接收新照片清空旧物体和结果卡片，取消旧请求，并用照片标识过滤迟到响应。

视觉重点是物体框选中反馈与菜单出现，采用简单描边、强调色和短过渡。主页面固定为竖屏，支持横向或纵向的输入照片。

## 6. 技术与数据约定

客户端使用 SwiftUI、系统相机包装和 PhotosPicker；复制使用系统剪贴板，分享使用系统分享面板。后端为一个 Node.js 服务，无第三方运行依赖，提供 `GET /health` 与 `POST /analyze`。

调用链：

`iPhone JPEG → 后端 /analyze → Gemini → 校验与坐标转换 → iOS 物体框和本地操作`

Gemini 密钥仅在后端环境中配置，模型由 `GEMINI_MODEL` 指定。开工阶段先验证账号实际可用的模型，不能把接口文档中的示例名称直接当成账号可用模型。

客户端将照片方向归一化，长边压缩到不超过 1600 像素；显示与上传使用同一份图像。后端将模型返回的 `[ymin,xmin,ymax,xmax]`、0–1000 坐标转换成相对整图的 0–1 `x/y/width/height`。客户端根据 aspectFit 的实际图片区域计算物体框，计入横向或纵向留白。[Gemini 图片与物体定位文档](https://ai.google.dev/gemini-api/docs/image-understanding)

请求：`imageBase64`、`mimeType`。响应格式示例，以下内容和坐标仅用于说明接口：

```json
{
  "objects": [
    {
      "id": "1",
      "name": "马克杯",
      "type": "object",
      "box": { "x": 0.2, "y": 0.1, "width": 0.4, "height": 0.5 },
      "description": "一个带把手的马克杯。",
      "usage": "常用于盛放饮品。",
      "text": null,
      "translation": null
    }
  ]
}
```

`type` 仅允许 `text`、`book`、`object`；缺失字段为 JSON `null`，无清晰物体返回空数组。框必须落在图片内且宽高大于零；不同物体的内容不能混用。

性能目标：照片选入后立即显示加载反馈；默认模型请求等待上限 30 秒，客户端 40 秒，超时给出重试入口。这是等待上限，不是已验证的识别速度承诺。菜单点击不等待第二次模型生成。

## 7. 单人 100 分钟排程

| 分钟 | 按海报对应时间 | 工作 | 阶段交付 |
| --- | --- | --- | --- |
| 0–8 | 15:40–15:48 | 验证 Xcode、运行设备、Gemini 配置和服务地址 | 确认环境前置条件，发起一次真实模型调用 |
| 8–25 | 15:48–16:05 | 系统拍照、相册、照片展示 | 两种来源都能展示同一张静态照片 |
| 25–45 | 16:05–16:25 | 接口、解析、坐标换算、物体框 | 真实识别结果能画到照片上 |
| 45–65 | 16:25–16:45 | 点击菜单、结果卡片、复制、分享 | 主流程完整可操作 |
| 65–80 | 16:45–17:00 | 两物体切换、重拍、失败重试、旧请求处理 | 连续运行两轮主流程 |
| 80–90 | 17:00–17:10 | 功能冻结，验证横竖图定位和菜单边界 | 修复影响演示的问题 |
| 90–100 | 17:10–17:20 | 排练、成功录屏、整理项目说明 | 可交付演示材料 |

取舍规则：

- 环境或密钥未就绪时继续源码、后端与可独立验证的工作，并明确真实运行尚未验证。
- 第 45 分钟优先跑通一个真实物体，再扩大到两个、最多三个。
- 第 65 分钟主流程仍有问题时停止视觉润色，集中修复选择物体、结果显示、复制和分享。
- 第 80 分钟冻结范围，最后 20 分钟留给稳定性和交付。
- 相机暂不可用时允许通过系统相册演示同一流程；这项降级要在交付中说明。

## 8. 参考海报与验收

参考素材为本项目 `reference-poster.jpg`。海报应识别为活动类文字物体，主要可见内容包括：

- TRAE on Campus · AI Coding Hackathon。
- 09 月 12 日 13:30–18:00；海报未标明年份。
- 香港城市大学杨建文学术楼 LT12。
- 闪电开发 15:40–17:20，共 100 分钟。

用户补充的遮挡文字是“携带好充满电的笔记本电脑和充电器”。这条是人工参考信息，纯图片识别不要求输出被遮挡的完整句子；应用不能写死该答案。完整时间基准见 `reference-facts.md`。

演示准备三种素材：参考海报、同框且不重叠的书和杯子、一张没有清晰文字的普通物体照片。实际图像结果以模型调用为准。

重点验收：

1. 海报名称、主要日期和地点基本正确，未猜测年份或遮挡内容。
2. 同一照片里两个清晰物体分别可点，菜单和内容互不串用。
3. 横图和竖图都有正确留白，框能点中对应物体。
4. 菜单在边缘物体附近出现时保持可见。
5. 复制与系统分享实际完成；取消分享可返回照片。
6. 模型超时、空结果、网络失败均有明确反馈。
7. 请求未完成时换图，最终只显示新照片的结果。

## 9. 演示顺序

建议内部排练控制在约一分钟，正式时长以主办方通知为准：

1. 开场：“电脑里的东西可以右键，现实世界为什么不可以？”
2. 拍摄或选择海报，等待真实识别，点击海报框查看摘要。
3. 复制文字或打开系统分享面板，证明结果可实际使用。
4. 换成书与杯子的同框照片，分别点击两个框展示不同菜单。
5. 用一句话解释：一次拍照返回多个物体及内容，物体选择决定当前操作。

演示录屏必须来自真实成功操作；测试中的固定响应只用于开发验证，不替代实际模型识别。


## 10. 追加授权：浏览器演示备用版

因首次安装 Xcode 需要额外下载 iOS 平台组件，用户明确授权同时制作手机浏览器备用版。保留原有 iOS 工程；浏览器版共享物体数据契约和识别后端，新增明确标注的人工交互示例，便于外部服务不可用时展示菜单交互。真实识别结果和人工示例必须始终区分。

用户随后提供智谱服务并配置密钥，后端新增智谱适配。原先 100 分钟是赛事预算，不因后续修复重新计时；项目不声称所有后续验证均在比赛窗口内完成。
# 状态建议更新

普通物体的“了解用途”调整为基于当前可见状态的处理、养护或使用建议；建议优先于观察说明，并进入分享内容。详细要求和实现边界见 [STATE-ADVICE.md](STATE-ADVICE.md)。
