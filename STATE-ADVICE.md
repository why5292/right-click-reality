# 根据物体当前状态给出建议

2026-09-14 更新。普通物体不再默认输出百科用途，先观察可见状态，再给出处理、养护或使用建议。没有有用建议时隐藏此项。建议放在菜单首项，观察内容在后；分享包含建议。

API 保留 usage 字段以兼容旧客户端，增加 usageTitle（处理建议、养护建议、使用建议、下一步建议）。旧版本可接收新的建议正文，新菜单标题需更新安装。海报与书籍的文字动作保持原有行为。

食品建议区分疑似霉斑、明显腐败与单纯氧化、磕碰。不因外观正常而保证安全，不输出与观察到的风险矛盾的食用用途。要求模型区分可见事实、推测和需要用户核实的信息。这不是确定性的食品安全检测。

食品处理原则参考 [USDA 食物霉菌说明](https://www.fsis.usda.gov/food-safety/safe-food-handling-and-preparation/food-safety-basics/molds-food-are-they-dangerous)。

## 同时完成的繁忙恢复

仍使用原 Gemini 模型和密钥。只有 HTTP 503 会在剩余时间足够时重试，最多共三次，间隔逐次增加，遵守 Retry-After；全程共用 60 秒上限，取消会停止等待。不会无限请求或替换为样例结果。重试增加请求次数，不保证上游恢复。

## 验证边界

- 21 项后端测试通过，覆盖建议跨后端与网页保留、物体内容隔离、分享、缺失标题兼容、503 恢复、次数上限、Retry-After 和取消。
- 18 项 Swift 核心检查通过，网络类型检查及完整真机签名构建成功。
- 新版已安装并成功启动于连接的 iPhone 15 Pro Max。
- 用用户提供的苹果截图调用原 Gemini，首次繁忙，加入重试后复测超时。尚未验证新提示词在原苹果照片上的实际内容质量。截图带有旧答案，不能作为无偏的原照片测试。
- 本地后端已重启。需要重新拍照或选择原照片，已有结果不会自动重新生成。
